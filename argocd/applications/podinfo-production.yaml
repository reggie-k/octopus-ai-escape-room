apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: podinfo-production
  namespace: argocd
  annotations:
    argo.octopus.com/project.values: octopus-ai-escape-room
    argo.octopus.com/environment.values: production
    argo.octopus.com/path.values: helm/podinfo
spec:
  project: default
  sources:
    - name: chart
      repoURL: https://stefanprodan.github.io/podinfo
      chart: podinfo
      targetRevision: "6.14.0"
      helm:
        valueFiles:
          - $values/helm/podinfo/values-production.yaml
    - name: values
      repoURL: https://github.com/reggie-k/octopus-ai-escape-room.git
      targetRevision: main
      ref: values
  destination:
    server: https://kubernetes.default.svc
    namespace: podinfo-production
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    syncOptions:
      - CreateNamespace=true
